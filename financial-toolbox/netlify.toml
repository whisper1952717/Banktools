[build]
  # 构建命令
  command = "npm run build"
  # 发布目录（Vite 默认输出到 dist）
  publish = "dist"

# SPA 路由重定向规则
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# 设置 Node.js 版本
[build.environment]
  NODE_VERSION = "18"
