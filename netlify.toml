[build]
  # 基础目录
  base = "financial-toolbox"
  # 构建命令
  command = "npm run build"
  # 发布目录（相对于 base 目录）
  publish = "dist"

# SPA 路由重定向规则
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# 设置 Node.js 版本
[build.environment]
  NODE_VERSION = "18"
