# 金融常用计算工具

一个集成了多个实用金融计算工具的网站，帮助用户直观了解不同理财方式的收益差距。

## 🎯 功能特性

### 1. 资产复利可视化计算器
- 直观对比单利（定存/国债）和复利（增额寿）的收益差距
- 可视化图表展示10年后的收益差异
- 支持自定义本金、利率和投资年限

### 2. IRR内部收益率计算器
- 一键测算投资项目的真实年化收益率
- 支持灵活的现金流输入（添加/删除条目）
- 自动生成专业的收益率解读

### 3. 家庭资产体检
- 7个维度全面评估家庭财务状况
- 智能生成风险评分和等级
- 提供个性化的资产配置建议参考

## 🚀 技术栈

- **前端框架**: React 18 + TypeScript
- **构建工具**: Vite
- **UI组件库**: Ant Design 5
- **图表库**: ECharts 5
- **路由**: React Router 6
- **样式**: CSS Modules + Tailwind CSS
- **精度计算**: Decimal.js

## 📱 设备支持

- ✅ 手机竖屏/横屏
- ✅ 平板电脑
- ✅ 桌面浏览器
- 采用移动优先设计，触控区域≥44x44px

## 🔒 隐私保护

- ✅ 所有计算在客户端完成
- ✅ 不向服务器发送任何用户数据
- ✅ 不使用Cookie或追踪技术
- ✅ 完全离线可用（部署后）

## 📦 安装与运行

### 环境要求

- Node.js >= 18.0.0
- npm >= 9.0.0

### 安装依赖

```bash
cd financial-toolbox
npm install
```

### 开发模式

```bash
npm run dev
```

访问 http://localhost:5173

### 生产构建

```bash
npm run build
```

构建产物将生成在 `dist` 目录。

### 预览生产构建

```bash
npm run preview
```

## 🌐 部署指南

### 部署到阿里云 ECS（推荐用于国内访问）

详细的阿里云部署指南请查看：
- **完整文档**: [DEPLOYMENT.md](./DEPLOYMENT.md)
- **快速指南**: [QUICK_DEPLOY.md](./QUICK_DEPLOY.md)

**快速部署步骤**：

1. **服务器配置**（首次部署）
```bash
# 上传配置脚本到服务器
scp financial-toolbox/server-setup.sh root@your-server-ip:/root/

# 在服务器上运行
ssh root@your-server-ip
chmod +x /root/server-setup.sh
/root/server-setup.sh
```

2. **配置部署脚本**（本地）
```bash
# 编辑 deploy.sh，修改服务器 IP
vi deploy.sh
# 修改 SERVER_IP="your-server-ip"

# 添加执行权限
chmod +x deploy.sh
```

3. **执行部署**
```bash
./deploy.sh
```

**系统要求**：
- 操作系统：Alibaba Cloud Linux 3 / CentOS 7+ / Ubuntu 20.04+
- 内存：1GB 以上
- 磁盘：10GB 以上

### 部署到 Vercel

1. 在 [Vercel](https://vercel.com) 注册账号
2. 导入 GitHub 仓库
3. Vercel 会自动检测 Vite 项目并配置构建
4. 点击部署即可

**构建配置**:
- Build Command: `npm run build`
- Output Directory: `dist`
- Install Command: `npm install`

### 部署到 Netlify

1. 在 [Netlify](https://netlify.com) 注册账号
2. 导入 GitHub 仓库
3. 配置构建设置：
   - Build command: `npm run build`
   - Publish directory: `dist`
4. 点击部署

**重要**: 需要添加 `_redirects` 文件以支持 SPA 路由：

```bash
# 在 public 目录创建 _redirects 文件
echo "/*    /index.html   200" > public/_redirects
```

### 部署到 GitHub Pages

1. 修改 `vite.config.ts`，添加 base 路径：

```typescript
export default defineConfig({
  base: '/your-repo-name/',
  // ... 其他配置
})
```

2. 构建并部署：

```bash
npm run build
# 使用 gh-pages 部署
npx gh-pages -d dist
```

## 📂 项目结构

```
financial-toolbox/
├── public/              # 静态资源
├── src/
│   ├── components/      # 通用组件
│   │   ├── Layout/      # 布局组件
│   │   ├── NumberInput/ # 数字输入组件
│   │   ├── RiskDisclaimer/ # 风险提示组件
│   │   └── InterestChart/  # 图表组件
│   ├── pages/           # 页面组件
│   │   ├── Home/        # 首页
│   │   ├── CompoundCalculator/ # 复利计算器
│   │   ├── IRRCalculator/      # IRR计算器
│   │   └── AssetAssessment/    # 资产体检
│   ├── utils/           # 工具函数
│   │   ├── calculations.ts  # 计算函数
│   │   ├── validators.ts    # 验证函数
│   │   ├── formatters.ts    # 格式化函数
│   │   └── assessment.ts    # 评估算法
│   ├── constants/       # 常量配置
│   │   ├── config.ts    # 全局配置
│   │   └── assessment.ts # 问卷配置
│   ├── types/           # TypeScript 类型定义
│   ├── App.tsx          # 主应用组件
│   └── main.tsx         # 入口文件
├── .kiro/               # Kiro 规格说明文档
│   ├── specs/
│   │   └── financial-toolbox/
│   │       ├── requirements.md  # 需求文档
│   │       ├── design.md        # 设计文档
│   │       └── tasks.md         # 任务列表
│   └── steering/
│       └── chinese-language.md  # 中文语言规范
├── package.json
├── vite.config.ts       # Vite 配置
├── tsconfig.json        # TypeScript 配置
└── README.md            # 项目文档
```

## 🧪 测试

项目采用属性测试（Property-Based Testing）和单元测试相结合的方式：

- 单元测试：验证特定示例和边缘情况
- 属性测试：验证通用属性在所有输入下都成立

```bash
# 运行测试（待实现）
npm run test
```

## 📝 开发规范

### 代码风格

- 使用 ESLint 和 Prettier 保持代码一致性
- 所有代码注释使用中文
- 变量和函数命名使用英文，但需添加中文注释

### 提交规范

```
feat: 新功能
fix: 修复bug
docs: 文档更新
style: 代码格式调整
refactor: 代码重构
perf: 性能优化
test: 测试相关
chore: 构建/工具链相关
```

## ⚠️ 免责声明

本工具仅供参考，不构成任何投资建议。投资有风险，决策需谨慎。请根据自身情况谨慎决策。

## 📄 许可证

MIT License

## 👥 贡献

欢迎提交 Issue 和 Pull Request！

## 📞 联系方式

如有问题或建议，请通过以下方式联系：

- 提交 GitHub Issue
- 发送邮件至：[your-email@example.com]

---

**版本**: 1.0.0  
**最后更新**: 2026年1月
